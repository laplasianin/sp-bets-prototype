server:
  port: ${PORT:8080}

spring:
  h2:
    console:
      enabled: false
  kafka:
    admin:
      fail-fast: false

app:
  cors:
    # DEMO ONLY — tighten to actual frontend URL before any real use
    allowed-origins: "*"
